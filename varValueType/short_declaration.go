package main

import "fmt"

// main operator
func main() {
	// Declare a variable and ASSIGN a Value of a certant TYPE
	// can not declar out side of a function body
	x := 42
	fmt.Println(x)
	// assign new value to var that is already declaried
	x = 99

	y := 100 + 24
	fmt.Println(y)
}
